# Keep delegation shallow and predictable. The root agent can choose among the
# project-scoped agents in .codex/agents/, but workers cannot recursively fan out.
[agents]
max_threads = 6
max_depth = 1
job_max_runtime_seconds = 1800
interrupt_message = true
