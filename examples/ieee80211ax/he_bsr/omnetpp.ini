[General]
network = inet.examples.ieee80211ax.he_bsr.HeBsrNetwork
sim-time-limit = 2s

**.constraintAreaMinX = 0m
**.constraintAreaMinY = 0m
**.constraintAreaMaxX = 620m
**.constraintAreaMaxY = 420m

**.ap.mobility.typename = "StationaryMobility"
**.host[*].mobility.typename = "StationaryMobility"
**.mobility.initFromDisplayString = false

**.ap.mobility.initialX = 300m
**.ap.mobility.initialY = 210m
**.host[0].mobility.initialX = 240m
**.host[0].mobility.initialY = 210m
**.host[1].mobility.initialX = 300m
**.host[1].mobility.initialY = 150m
**.host[2].mobility.initialX = 360m
**.host[2].mobility.initialY = 210m

**.ap.wlan[*].address = "10:00:00:00:00:00"
**.host[*].**.mgmt.accessPointAddress = "10:00:00:00:00:00"
**.arp.typename = "GlobalArp"
*.radioMedium.sameTransmissionStartTimeCheck = "ignore"

**.opMode = "ax"
**.bandName = "5 GHz (20 MHz)"
**.centerFrequency = 5GHz
**.wlan[*].bitrate = 14.625Mbps
**.wlan[*].radio.transmitter.power = 10mW
**.wlan[*].radio.receiver.sensitivity = -85dBm
**.wlan[*].radio.receiver.snirThreshold = 4dB

**.mac.qosStation = true
**.wlan[*].classifier.typename = "ExampleQosClassifier"
**.mac.hcf.typename = "HeHcf"
**.wlan[*].mac.hcf.enableUlMuOfdma = true
**.ap.wlan[*].mac.hcf.ulScheduler.typename = "HeUlSchedulerBacklogBased"
**.ap.wlan[*].mac.hcf.ulScheduler.minRandomAccessRus = 0
**.ap.wlan[*].mac.hcf.ulScheduler.maxRandomAccessRus = 1
**.ap.wlan[*].mac.hcf.ulScheduler.defaultMcs = 2
**.isBlockAckSupported = true

**.server.numApps = 1
**.server.app[0].typename = "UdpSink"
**.server.app[0].localPort = 5000
**.host[*].numApps = 2

# Stage 1: Warmup Trigger (App 0) to establish ADDBA agreements
**.host[*].app[0].typename = "UdpBasicApp"
**.host[*].app[0].destAddresses = "server"
**.host[*].app[0].destPort = 5000
**.host[*].app[0].messageLength = 700B
**.host[*].app[0].sendInterval = 1s
**.host[*].app[0].startTime = 0.2s
**.host[*].app[0].stopTime = 0.25s

# Stage 2: Saturated Data Phase (App 1)
**.host[*].app[1].typename = "UdpBasicApp"
**.host[*].app[1].destAddresses = "server"
**.host[*].app[1].destPort = 5000
**.host[*].app[1].messageLength = 700B
**.host[*].app[1].sendInterval = 0.35ms
**.host[*].app[1].startTime = 0.3s

[Config FullBsrAccounting]
description = "Full HE BSR parsing/accounting drives AP-side UL OFDMA scheduling from fresh per-AC queue reports."

[Config StaleBsr]
description = "A short, stable BSR freshness window forces repeated BSRP polling and exposes stale-report scheduling behavior."
# 1 ms can expire reports while the AP is still completing the preceding
# trigger exchange, leaving HeHcf in a transmit-step timeout.  10 ms remains
# much shorter than the normal reporting horizon while allowing one exchange
# to complete deterministically.
**.ap.wlan[*].mac.hcf.ulCoordinator.reportMaxAge = 10ms

[Config ImplicitBsr]
description = "STAs transmit BSRs implicitly via uplink SU QoS Data frames. AP schedules UL OFDMA without BSRP triggers."
**.ap.wlan[*].mac.hcf.ulTriggerCheckInterval = 0.5s

[Config BurstyTraffic]
description = "Bursty Uplink Traffic: Hosts generate traffic in separate ON/OFF bursts, forcing dynamic explicit BSRP polls when backlog restarts."
extends = FullBsrAccounting
# Configure 2 apps per host to create an ON/OFF traffic pattern
**.host[*].numApps = 2
**.host[*].app[*].typename = "UdpBasicApp"
**.host[*].app[*].destAddresses = "server"
**.host[*].app[*].destPort = 5000
**.host[*].app[*].messageLength = 700B
**.host[*].app[*].sendInterval = 0.35ms
# First burst (ON phase: 0.3s to 0.5s)
**.host[*].app[0].startTime = 0.3s
**.host[*].app[0].stopTime = 0.5s
# Second burst (ON phase: 1.0s to 1.3s)
**.host[*].app[1].startTime = 1.0s
**.host[*].app[1].stopTime = 1.3s
# Sinks must listen on the same port
**.server.numApps = 1
**.server.app[0].typename = "UdpSink"
**.server.app[0].localPort = 5000

