# TWT regression scenarios

`Baseline`, `IndividualUnannounced`, `IndividualAnnounced`, and `Broadcast`
use identical offered traffic and seed. Compare the STA `twtAwakeTime` and
`twtSleepTime` scalars with delivered-packet scalars; where an experiment adds
a radio energy consumer, compare its consumed-energy scalar under the same
four configurations.
