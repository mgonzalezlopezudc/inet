---
phase: 01-addba-validation-handshake-correctness
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tests/unit/Ieee80211HeMuAddbaValidation_1.test
  - src/inet/linklayer/ieee80211/mac/coordinationfunction/HeHcf.cc
  - src/inet/linklayer/ieee80211/mac/coordinationfunction/Hcf.cc
  - src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreement.h
  - src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementHandler.cc
  - src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementPolicy.cc
  - src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementPolicy.ned
autonomous: true
requirements:
  - MAC-01
  - MAC-02
requirements_addressed:
  - MAC-01
  - MAC-02
must_haves:
  truths:
    - "MAC-02/D-01: A STA lacking an active receiver/TID BA agreement is served through standard single-user EDCA transmission, not DL MU OFDMA."
    - "D-02: Failed or timed-out ADDBA setup attempts are retried in bounded bursts of up to 3 attempts, then held until the concrete cooldown expires; retries may resume after cooldown without agreement teardown/recreation."
    - "D-03/D-04: ADDBA Requests continue to use the existing single-user management path and are classified as AC_VO by `Hcf::processUpperFrame()`."
    - "D-06: `HeHcf::collectCandidateStations()` validates only the first packet for each destination/TID and does not let later packets bypass an ineligible head packet for that destination."
    - "D-08/D-09: `HeHcf` dynamically queries the originator BA handler at each TXOP so DELBA/timeout state immediately affects the next scheduling decision."
    - "D-11: After a new originator BA agreement is established and pending packets exist, `Hcf` triggers contention/access checks promptly with `resumeContention()`."
    - "Planning assumption for coexistence: if the pending queue's earliest SU-transmittable packet is not MU-eligible, this TXOP uses SU fallback even when later packets could form a valid MU group."
  artifacts:
    - path: "tests/unit/Ieee80211HeMuAddbaValidation_1.test"
      provides: "MAC-02 fallback, ADDBA retry/cooldown, and contention-resume coverage"
      contains: "fallback"
    - path: "src/inet/linklayer/ieee80211/mac/coordinationfunction/HeHcf.cc"
      provides: "FIFO-preserving MU candidate filtering and SU fallback trigger"
      contains: "Hcf::startFrameSequence(ac)"
    - path: "src/inet/linklayer/ieee80211/mac/coordinationfunction/Hcf.cc"
      provides: "Prompt contention resume after successful ADDBA response"
      contains: "resumeContention"
    - path: "src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementPolicy.ned"
      provides: "Concrete ADDBA retry cooldown parameter"
      contains: "addbaFailureTimeout"
  key_links:
    - from: "src/inet/linklayer/ieee80211/mac/coordinationfunction/HeHcf.cc"
      to: "src/inet/linklayer/ieee80211/mac/coordinationfunction/Hcf.cc"
      via: "`Hcf::startFrameSequence(ac)` SU fallback"
      pattern: "Hcf::startFrameSequence\\(ac\\)"
    - from: "src/inet/linklayer/ieee80211/mac/coordinationfunction/Hcf.cc"
      to: "src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementHandler.cc"
      via: "`originatorBlockAckAgreementHandler->processTransmittedDataFrame(...)`"
      pattern: "processTransmittedDataFrame"
---

<objective>
Implement FIFO-preserving single-user fallback and ADDBA lifecycle guards for MAC-02 while keeping MAC-01 candidate filtering dynamic, per D-01 through D-04 and D-06 through D-11.

Purpose: `HeHcf` decides whether a TXOP should start a DL MU sequence or delegate to the existing SU HCF path. The SU path must remain responsible for ADDBA management frames and AC_VO classification.
Output: Extended focused tests, HE candidate filtering/fallback changes, prompt contention resume after ADDBA response, and bounded ADDBA retry/cooldown behavior.
</objective>

## Phase Goal

**As a** simulator maintainer, **I want to** fall back to standard single-user EDCA when DL MU Block Ack prerequisites are missing, **so that** packets are delivered without being dropped or reordered and the existing ADDBA handshake path prepares future MU service.

<execution_context>
@/home/user/omnetpp_ws/inet/.codex/gsd-core/workflows/execute-plan.md
@/home/user/omnetpp_ws/inet/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-addba-validation-handshake-correctness/01-CONTEXT.md
@.planning/phases/01-addba-validation-handshake-correctness/01-RESEARCH.md
@.planning/phases/01-addba-validation-handshake-correctness/01-VALIDATION.md
@.planning/phases/01-addba-validation-handshake-correctness/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/inet/linklayer/ieee80211/mac/blockack/BlockAckAgreementUtils.h
@src/inet/linklayer/ieee80211/mac/coordinationfunction/HeHcf.cc
@src/inet/linklayer/ieee80211/mac/coordinationfunction/HeHcf.h
@src/inet/linklayer/ieee80211/mac/coordinationfunction/Hcf.cc
@src/inet/linklayer/ieee80211/mac/coordinationfunction/Hcf.h
@src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementHandler.cc
@src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementPolicy.cc
@src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementPolicy.ned
@tests/unit/Ieee80211HeMuAddbaValidation_1.test
</context>

## Artifacts this phase produces

- Additional scenarios in `tests/unit/Ieee80211HeMuAddbaValidation_1.test` for MAC-02 fallback and ADDBA retry/cooldown.
- File-local helper(s) in `HeHcf.cc` for earliest-packet MU eligibility and FIFO-preserving fallback decisions.
- `OriginatorBlockAckAgreementPolicy.ned` parameter `addbaFailureTimeout`.
- Implemented `OriginatorBlockAckAgreementPolicy::computeAddbaFailureTimeout()`.
- Bounded ADDBA retry/cooldown cycle in `OriginatorBlockAckAgreementHandler::processTransmittedDataFrame(...)`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend focused tests for SU fallback and ADDBA retry behavior</name>
  <files>tests/unit/Ieee80211HeMuAddbaValidation_1.test</files>
  <read_first>
    tests/unit/Ieee80211HeMuAddbaValidation_1.test
    src/inet/linklayer/ieee80211/mac/coordinationfunction/HeHcf.cc
    src/inet/linklayer/ieee80211/mac/coordinationfunction/Hcf.cc
    src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementHandler.cc
    src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementPolicy.cc
    src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementPolicy.ned
    .planning/phases/01-addba-validation-handshake-correctness/01-VALIDATION.md
    .planning/phases/01-addba-validation-handshake-correctness/01-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: If the pending queue's earliest SU-transmittable packet lacks an active BA agreement, the expected scheduling result is SU fallback even when two later STAs are MU-eligible.
    - Test 2: With fewer than two active-BA eligible candidates after head-of-line filtering, `HeHcf` delegates to `Hcf::startFrameSequence(ac)`.
    - Test 3: SU data transmission keeps `OriginatorBlockAckAgreementHandler::processTransmittedDataFrame(...)` on the path so an ADDBA Request can be enqueued through management processing and classified AC_VO.
    - Test 4: ADDBA request retries are capped at 3 attempts per bounded burst, suppress additional requests until `addbaFailureTimeout` elapses, then allow a later retry for the same receiver/TID without agreement teardown/recreation.
  </behavior>
  <action>Extend `Ieee80211HeMuAddbaValidation_1.test` instead of creating a second test file. Keep Plan 01 MAC-01 cases intact. Add deterministic output assertions for fallback selection, no packet drop/reorder, ADDBA request enqueue through the existing `Hcf`/handler path, and the D-02 retry cycle: attempts 1 through 3 form a bounded burst, further attempts before `addbaFailureTimeout` are suppressed, and attempt 4 is allowed after the cooldown for the same receiver/TID without tearing down or recreating the inactive agreement. The test should be RED before Tasks 2 and 3. If a full `HeHcf` module fixture is too heavy, keep the `.test` focused on the smallest executable harness and supplement with source assertions in the test output; do not replace the automated `.test` with manual verification.</action>
  <acceptance_criteria>
    - The test file contains MAC-02 cases that mention `Hcf::startFrameSequence`, `processTransmittedDataFrame`, `AddbaReq`, `AC_VO`, `addbaFailureTimeout`, bounded burst limit `3`, and a post-cooldown retry without teardown.
    - The test verifies that ineligible packets remain in pending order for SU fallback and are not silently dropped.
    - The quick command fails before production changes and passes after Tasks 2 and 3.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'source setenv && export PYTHONPATH=/home/user/omnetpp-6.4.0/python:$PYTHONPATH && python3 -c "from inet.test.opp import run_opp_tests; run_opp_tests(\"tests/unit\", filter=\"Ieee80211HeMuAddbaValidation_1.test\", full_match=True)"'</automated>
  </verify>
  <done>The focused test covers MAC-02/D-01 through D-04 and D-06 through D-11 and is ready to prove the fallback implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route missing-agreement head traffic through SU fallback</name>
  <files>src/inet/linklayer/ieee80211/mac/coordinationfunction/HeHcf.cc, src/inet/linklayer/ieee80211/mac/coordinationfunction/Hcf.cc</files>
  <read_first>
    src/inet/linklayer/ieee80211/mac/blockack/BlockAckAgreementUtils.h
    src/inet/linklayer/ieee80211/mac/coordinationfunction/HeHcf.cc
    src/inet/linklayer/ieee80211/mac/coordinationfunction/HeHcf.h
    src/inet/linklayer/ieee80211/mac/coordinationfunction/Hcf.cc
    src/inet/linklayer/ieee80211/mac/coordinationfunction/Hcf.h
    src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementHandler.cc
    .planning/phases/01-addba-validation-handshake-correctness/01-CONTEXT.md
    .planning/phases/01-addba-validation-handshake-correctness/01-RESEARCH.md
  </read_first>
  <behavior>
    - A head-of-queue packet that is not unicast `ST_DATA_WITH_QOS` with active receiver/TID BA agreement prevents MU scheduling for that TXOP and uses SU fallback.
    - Candidate collection uses the first packet seen for each destination and never promotes a later packet for the same destination over an ineligible head packet.
    - Successful ADDBA response updates cause pending queues to get prompt contention/access checks for subsequent TXOPs.
  </behavior>
  <action>Include `BlockAckAgreementUtils.h` in `HeHcf.cc`. In `HeHcf::startFrameSequence(ac)`, after existing in-progress requeue logic and before scheduler use, inspect the pending queue's earliest SU-transmittable packet. If that packet is not unicast `ST_DATA_WITH_QOS` with an active BA agreement for its receiver/TID, log the fallback decision and call `Hcf::startFrameSequence(ac)` for this TXOP, then return. This implements the planning assumption that missing-agreement head traffic gets SU service even if later MU-eligible packets exist. In `HeHcf::collectCandidateStations(queue)`, keep first-seen destination order, use `hasActiveOriginatorBlockAckAgreement`, and do not cache results across TXOPs. In `Hcf::recipientProcessReceivedManagementFrame(...)` and `Hcf::originatorProcessReceivedManagementFrame(...)`, after a successful ADDBA response update and `blockAckAgreementAddedSignal` emit, call `resumeContention()` so pending traffic can be reconsidered promptly per D-11. Preserve the existing ADDBA Request creation and AC_VO classification path; do not enqueue management frames directly from `HeHcf`.</action>
  <acceptance_criteria>
    - `HeHcf::startFrameSequence(ac)` contains a direct `Hcf::startFrameSequence(ac)` fallback before `HeDlMuTxOpFs` construction when the earliest SU-transmittable packet is MU-ineligible.
    - `HeHcf::collectCandidateStations` calls `hasActiveOriginatorBlockAckAgreement` and still records first-seen destinations so later packets cannot bypass an ineligible first packet for the same destination.
    - `Hcf` calls `resumeContention()` after originator ADDBA response success paths without changing ADDBA Request construction or `processMgmtFrame()` AC_VO classification.
    - Focused ADDBA validation tests pass.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'source setenv && export PYTHONPATH=/home/user/omnetpp-6.4.0/python:$PYTHONPATH && python3 -c "from inet.test.opp import run_opp_tests; run_opp_tests(\"tests/unit\", filter=\"Ieee80211HeMuAddbaValidation_1.test\", full_match=True)"'</automated>
    <automated>bash -lc 'grep -n "Hcf::startFrameSequence(ac)" src/inet/linklayer/ieee80211/mac/coordinationfunction/HeHcf.cc && grep -n "hasActiveOriginatorBlockAckAgreement" src/inet/linklayer/ieee80211/mac/coordinationfunction/HeHcf.cc && grep -n "resumeContention" src/inet/linklayer/ieee80211/mac/coordinationfunction/Hcf.cc'</automated>
  </verify>
  <done>MAC-02 fallback is selected before MU scheduling can bypass missing-agreement head traffic, and new BA agreement success prompts contention checks for queued packets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bound ADDBA retry attempts with a concrete cooldown</name>
  <files>src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreement.h, src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementHandler.cc, src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementPolicy.cc, src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementPolicy.ned</files>
  <read_first>
    src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementHandler.cc
    src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementHandler.h
    src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreement.h
    src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementPolicy.cc
    src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementPolicy.h
    src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementPolicy.ned
    src/inet/linklayer/ieee80211/mac/coordinationfunction/Hcf.cc
    .planning/phases/01-addba-validation-handshake-correctness/01-CONTEXT.md
  </read_first>
  <behavior>
    - First SU data transmission with no agreement creates and enqueues an ADDBA Request through the existing handler path.
    - Existing inactive agreement retries ADDBA only when the current bounded burst has capacity or the previous burst's cooldown has elapsed.
    - After 3 attempts in a bounded burst, additional SU data transmissions do not flood ADDBA Requests for the same receiver/TID before cooldown; after `addbaFailureTimeout` elapses, retrying resumes without agreement teardown/recreation.
  </behavior>
  <action>Add `double addbaFailureTimeout @unit(s) = default(1s);` to `OriginatorBlockAckAgreementPolicy.ned`, initialize `addbaFailureTimeout` from that parameter, and implement `computeAddbaFailureTimeout()` to return it. In `OriginatorBlockAckAgreementHandler::processTransmittedDataFrame(...)`, preserve first-request creation when `agreement == nullptr`. When an agreement exists but `getIsAddbaResponseReceived()` is false, treat `getNumSentBaPolicyFrames()` as the ADDBA request attempt counter for the current inactive agreement and enforce D-02 as a cooldown cycle, not a lifetime stop: enqueue another ADDBA Request only when the inactive agreement's retry deadline has elapsed; after every transmitted ADDBA Request, set the inactive agreement's temporary `blockAckTimeoutValue` to `blockAckAgreementPolicy->computeAddbaFailureTimeout()` and recalculate `expirationTime` as the not-before-next-retry deadline. Use the attempt count to cap any immediate bounded burst at 3 attempts and to log when a burst is exhausted, but allow the next burst after `addbaFailureTimeout` without requiring agreement teardown/recreation. Do not add a separate timer, global retry map, or MU-specific agreement map; if a narrow helper on `OriginatorBlockAckAgreement` is needed to keep burst/cooldown state readable, add it there and keep the state owned by the agreement. In `processTransmittedAddbaReq(...)`, call `agreement->baPolicyFrameSent()` when the request is transmitted and update the retry deadline for inactive agreements. When `updateAgreement(...)` receives an ADDBA response, let the existing negotiated Block Ack timeout overwrite the temporary retry cooldown state. Log suppressed retries with `EV_DETAIL` or `EV_WARN` using receiver/TID, attempt count, and retry deadline.</action>
  <acceptance_criteria>
    - `OriginatorBlockAckAgreementPolicy.ned` defines `addbaFailureTimeout` with `@unit(s)` and default `1s`.
    - `OriginatorBlockAckAgreementPolicy::computeAddbaFailureTimeout()` no longer throws `cRuntimeError("Unimplemented")`.
    - `OriginatorBlockAckAgreementHandler::processTransmittedDataFrame(...)` enforces a maximum of 3 ADDBA attempts per bounded burst for inactive agreements, suppresses retries before cooldown expiry, and allows a later retry after cooldown without agreement teardown/recreation.
    - No new ADDBA scheduler, timer subsystem, or MU-specific agreement map is introduced.
    - Focused ADDBA validation tests pass.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'source setenv && export PYTHONPATH=/home/user/omnetpp-6.4.0/python:$PYTHONPATH && python3 -c "from inet.test.opp import run_opp_tests; run_opp_tests(\"tests/unit\", filter=\"Ieee80211HeMuAddbaValidation_1.test\", full_match=True)"'</automated>
    <automated>bash -lc 'grep -n "addbaFailureTimeout" src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementPolicy.ned src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementPolicy.cc && grep -n "baPolicyFrameSent" src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementHandler.cc && grep -n "getNumSentBaPolicyFrames" src/inet/linklayer/ieee80211/mac/blockack/OriginatorBlockAckAgreementHandler.cc'</automated>
  </verify>
  <done>ADDBA fallback remains on the existing SU management path while missing-response retries use a 3-attempt bounded burst followed by a concrete cooldown, then resume after cooldown without teardown.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EDCA pending queue -> HeHcf | Queue contents determine MU vs SU TXOP selection and may contain invalid headers, non-QoS frames, or missing-agreement traffic. |
| HeHcf -> Hcf SU path | Fallback hands control from HE-specific scheduling to standard single-user frame sequencing. |
| ADDBA handler state -> future MU eligibility | Request, response, DELBA, timeout, retry, and cooldown state affects future DL MU scheduling eligibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | `HeHcf::collectCandidateStations()` | mitigate | Validate first-seen destination head packet with active receiver/TID BA predicate at each TXOP. |
| T-01-06 | Denial of Service | Null BA handler or inactive agreement | mitigate | Use SU fallback through `Hcf::startFrameSequence(ac)` instead of dereferencing or dropping. |
| T-01-07 | Denial of Service | Missing-agreement head packet starvation | mitigate | FIFO assumption: earliest SU-transmittable MU-ineligible packet forces SU fallback for the current TXOP. |
| T-01-08 | Tampering | Stale agreement after DELBA/timeout | mitigate | Avoid cached candidates and query the BA handler dynamically before every MU scheduling attempt. |
| T-01-09 | Denial of Service | ADDBA request flood | mitigate | Enforce a 3-attempt bounded burst and `addbaFailureTimeout` cooldown cycle for inactive agreements. |
| T-01-SC | Tampering | Package installs | accept | No npm/pip/cargo package installation is planned for this C++/OMNeT++ phase. |
</threat_model>

<verification>
Run after all tasks:
<automated>bash -lc 'source setenv && export PYTHONPATH=/home/user/omnetpp-6.4.0/python:$PYTHONPATH && python3 -c "from inet.test.opp import run_opp_tests; run_opp_tests(\"tests/unit\", filter=\"Ieee80211HeMuAddbaValidation_1.test\", full_match=True)"'</automated>
<automated>bash -lc 'source setenv && export PYTHONPATH=/home/user/omnetpp-6.4.0/python:$PYTHONPATH && python3 -c "from inet.test.opp import run_opp_tests; run_opp_tests(\"tests/unit\", filter=\"Ieee80211HeMuSeqAck_1.test\", full_match=True)"'</automated>
</verification>

<success_criteria>
- MAC-01 and MAC-02 are both covered by the focused ADDBA validation test.
- Missing or inactive BA agreement traffic uses `Hcf::startFrameSequence(ac)` and remains eligible for existing ADDBA Request creation.
- ADDBA Requests remain classified through existing management-frame AC_VO handling.
- Dynamic BA state changes affect the next TXOP, while active MU sequences are not disrupted mid-sequence.
- ADDBA retry behavior is bounded to 3 attempts per burst with a concrete cooldown, resumes after cooldown without agreement teardown, and adds no new management scheduling subsystem.
</success_criteria>

<output>
Create `.planning/phases/01-addba-validation-handshake-correctness/01-02-SUMMARY.md` when done.
</output>
