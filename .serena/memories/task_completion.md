# Task completion
- For targeted 802.11 HE changes, source OMNeT++ and INET environments, set `CCACHE_DISABLE=1`, then run the relevant unit-test filter.
- Inspect the final diff to ensure no unrelated generated/build files are included.